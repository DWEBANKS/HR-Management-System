@model IEnumerable<WebApplication1hrsystem.Models.Salary>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Salary1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.From_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.To_date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Salary1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.From_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.To_date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Emp_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Emp_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Emp_id })
        </td>
    </tr>
}

</table>
